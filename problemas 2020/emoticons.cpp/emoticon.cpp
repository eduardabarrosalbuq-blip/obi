#include<iostream>
#include<string>

using namespace std;

int main(void){
    string linha;
int D= 0, C =0

    getline (cin, linha);

    for(basic string<char> i = linha.begin(); (i < linha.end()-2); i++){
if( linh[i] == ':' && linha[i+1] == '-' && linha[i+2] == ')'){
D++
}
if( linh[i] == ':' && linha[i+1] == '-' && linha[i+2] == '('){
C++
 }
 if (D > c){
    cout << "divertido\n";
 }else if (D < C);

    cout << linha << "\n";

return 0;
}